// Bottom padding scroll containers in the (tabs) routes need so the last item
// clears the floating pill tab bar. Sized for: bar bottom inset (8) + bar height
// (56) + breathing (16) + iPhone home indicator (~34, varies by device) = 114,
// rounded up to 120 for a little extra headroom.
export const TAB_BAR_SCROLL_PADDING = 120;
